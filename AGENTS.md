# 컨벤션 저장소 작업 지침

## 목적과 범위

이 폴더는 사용자가 여러 프로젝트에서 일관되게 사용할 개인 개발 컨벤션을 정리하는 공간이다. 우선 C, C++, Git을 다루고 필요에 따라 확장한다. 현업에서 공개적으로 사용하는 가이드와 공식 문서를 참고하되, 기존 사용자의 스타일과 실제 개발 환경에 맞춰 선택한다.

이 파일은 컨벤션을 조사·작성·수정하는 작업 원칙이다. 구체적인 규칙은 항목별 페이지를 연결하는 개인 위키 형태로 작성한다. 향후 개인 사이트로도 탐색할 수 있도록 문서 구조와 링크를 설계한다. 사용자의 명시적인 요청을 우선하며, 아직 결정하지 않은 스타일을 확정된 개인 규칙처럼 서술하지 않는다.

## 위키 구조와 사이트 확장

- 한 페이지는 독립적으로 찾아보고 이해할 수 있는 주제 하나를 다룬다. 예를 들어 C++의 명명, 초기화, 소유권과 Git의 커밋 메시지, 브랜치, 병합 전략을 각각 분리한다.
- 서로 밀접한 짧은 규칙은 한 페이지 안의 절로 묶는다. 규칙 한 줄마다 파일을 만들거나 같은 설명을 여러 페이지에 반복하지 않는다.
- 루트 `README.md`는 위키의 입구로, `docs/`는 새로 정리하는 문서의 기준 위치로 사용한다. 각 분류의 `index.md`에는 소개, 읽는 순서와 하위 페이지 링크를 둔다.
- 탐색 경로는 기본적으로 `홈 → 분류 → 항목`으로 구성한다. 항목 페이지에는 상위 목차와 관련 항목으로 가는 링크를 둔다. 새 페이지를 만들면 해당 분류 목차에도 등록한다.
- 디렉터리와 파일 이름은 짧고 의미 있는 영문 소문자 및 `kebab-case`를 사용한다. 화면에 표시할 제목과 설명은 한국어로 작성한다. 문서 경로에는 날짜나 정렬용 번호를 넣지 않아 링크를 안정적으로 유지한다.
- 문서 내부 링크는 `/` 구분자를 사용하는 상대 경로로 작성한다. 로컬 절대 경로, 특정 편집기 전용 링크와 위키 전용 링크 문법에 의존하지 않는다.
- 본문은 일반 Markdown을 기본으로 작성한다. 사이트 구현 전에는 특정 프레임워크의 컴포넌트나 필수 메타데이터 형식을 도입하지 않는다. 사이트화할 때 기존 Markdown을 원본으로 사용하고 별도의 본문 사본을 만들지 않는다.
- 사이트의 디자인, 검색, 배포 도구는 실제 사이트 작업 시 선택한다. 현재 문서 정리 단계에서는 탐색하기 쉬운 구조와 내용의 일관성에 집중한다.

아래는 현재 위키의 대분류 구조다. 세부 항목은 후속 작업에서 실제 내용에 맞춰 추가한다. C와 C++의 언어별 문서는 `coding/` 아래에 구성한다. 기존 자료는 `ref/`에서 참고한다.

```text
README.md
AGENTS.md
docs/
  index.md
  coding/
    index.md
  design/
    index.md
  git/
    index.md
  quality/
    index.md
  build/
    index.md
  documentation/
    index.md
  projects/
    index.md
```

공통 규칙이 필요하면 공통 분류를 추가하고 C와 C++ 페이지에서 연결한다. 공통화는 실제로 같은 규칙에만 적용한다. 기존의 종합 `CodingConvention.md` 형식을 새 문서의 기본 형식으로 사용하지 않는다.

## 기존 자료 취급

- `ref/CodingConvention.md`: 기존 DevMiniEngine 코딩 컨벤션.
- `ref/DevRnimanConvention/`: 기존 C++·DX12 및 한글·영문 Git 컨벤션 자료.
- `ref/gameConvention/`: DX12 프로젝트 컨벤션과 내보내기 압축 자료.
- 그 밖의 기존 자료는 `ref/` 아래에서 확인한다.

기존 문서는 개인 선호와 과거 결정의 근거로 읽는다. 서로 다른 문서의 내용이 일치하거나 최신이라는 가정은 하지 않는다. 파일명이나 수정 시각만으로 기준 문서를 결정하지 않는다.

원문을 임의로 삭제하거나 덮어쓰지 않는다. 통합 작업에서는 중복·충돌·프로젝트 전용 규칙을 먼저 파악하고, 어떤 규칙을 유지하거나 변경했는지 추적할 수 있게 한다. 관련 없는 파일과 압축 자료는 작업상 필요가 없으면 변경하지 않는다.

## 참고 자료와 근거

우선 참고할 자료는 다음과 같다. 아래 목록은 참고 후보이며 해당 가이드 전체를 채택한다는 뜻은 아니다.

| 자료 | 참고 목적 |
| --- | --- |
| [Google C++ Style Guide](https://google.github.io/styleguide/cppguide.html) | C++ 명명, 포맷, 파일 구성 및 기능 사용 정책 비교 |
| [C++ Core Guidelines](https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines) | C++ 설계, 타입 안전성, 자원 관리 및 인터페이스 원칙 |
| [Git 공식 문서](https://git-scm.com/docs) | Git 명령과 동작 확인 |
| [Pro Git](https://git-scm.com/book/en/v2) | 브랜치, 협업 및 이력 관리 방식 비교 |
| [Conventional Commits 1.0.0](https://www.conventionalcommits.org/en/v1.0.0/) | 커밋 메시지 형식 후보 |

- 외부 규칙을 도입할 때 원문을 확인하고 관련 절의 링크와 확인일을 기록한다. 버전이 있는 명세는 버전도 명시한다.
- 공식 문서와 원저자의 공개 가이드를 우선한다. 블로그나 번역은 보조 자료로 사용한다.
- 특정 회사의 정책, 언어 표준의 요구사항, 일반적인 권장 사항, 개인 취향을 구별한다. 근거 없이 “업계 표준”이나 “현업에서 항상 사용”이라고 단정하지 않는다.
- 출처의 권고와 이 저장소에서 채택한 규칙을 분리한다. 외부 가이드와 다르게 정했다면 차이와 이유를 적는다.
- 원문을 대량 복사하지 않고 필요한 내용을 한국어로 요약한다.

## 규칙을 결정하는 방법

현재 채택 기준은 [컨벤션 적용 기준](docs/baseline.md)이다. 이 목록의 기존 규칙을 새 제안과 구분하고, 개정 시 해당 문서와 기준 문서의 변경 기록을 갱신한다. 미작성·후속 항목까지 채택된 것으로 해석하지 않는다.

1. 관련 기존 문서를 읽고 현재 스타일과 충돌 지점을 정리한다.
2. 해당 주제의 공식 자료를 확인하고 필요한 대안을 비교한다.
3. 정확성, 안전성, 가독성, 유지보수성, 도구 적용 가능성을 기준으로 기본안을 제안한다.
4. 규칙마다 채택 이유와 적용 범위를 적고 예외는 구체적인 조건으로 제한한다.
5. 문서 본문·예제·요약·도구 설정에 같은 결정을 반영한다.

명확한 기존 선호는 유지하는 것을 기본으로 한다. 충돌하거나 근거가 부족한 경우 합리적인 초안을 작성하고 미결정 사항을 표시한다. 중요한 선택이 사용자 정보에 달려 있을 때만 핵심 질문을 묻고, 독립적으로 가능한 작업은 계속한다.

상태는 `초안`, `채택`, `보류`, `폐기`로 구분한다. 사용자의 선택이나 명확한 기존 근거가 없는 새 제안을 임의로 `채택` 처리하지 않는다. 문서 편집 같은 일상적인 작업마다 승인을 요청할 필요는 없다.

## 주제별 구분

### C와 C++

- 공통 포맷과 언어별 규칙을 구분한다. C++ 규칙을 C에 그대로 적용하지 않는다.
- 언어 표준 버전과 플랫폼·컴파일러 제약을 명시한다. 아직 정해지지 않은 버전을 임의로 확정하지 않는다.
- 명명·들여쓰기·중괄호 같은 스타일과 수명·소유권·오류 처리 같은 설계 원칙을 구분한다.
- DX12, Windows, COM, PCH 등 기존 프로젝트에 종속된 규칙은 범용 규칙과 구분한다.
- 성능 규칙에는 적용 조건과 근거를 적는다. 측정 없이 특정 문법이 항상 빠르다고 단정하지 않는다.

### Git

- 커밋 메시지, 브랜치 명명, 병합 전략, 리뷰 절차를 별도 항목으로 다룬다.
- 기존 `[Add]`, `[Fix]` 방식과 Conventional Commits 형식은 비교 후 선택한다. 동일 범위의 기본 형식으로 둘을 혼용하지 않는다.
- 커밋 제목·본문의 언어 정책은 설명 문서의 언어 정책과 별도로 정한다.
- 개인 프로젝트와 팀 프로젝트의 작업 흐름 차이를 고려한다. 실제 팀의 합의가 필요한 절차를 보편적인 의무로 만들지 않는다.
- 컨벤션 작성 요청만으로 실제 커밋, 푸시, 이력 재작성 또는 저장소 설정 변경을 수행하지 않는다.

## 문서 작성 형식

설명은 한국어로 작성하고 식별자, 명령어, API 이름은 원래 표기를 유지한다. 각 주제 문서는 아래 내용을 필요한 만큼 포함한다.

- 목적, 적용 범위, 상태와 대상 버전.
- 바로 찾아볼 수 있는 핵심 규칙 요약.
- 구체적인 규칙과 이유.
- 올바른 예시와, 차이가 명확해지는 경우 피해야 할 예시.
- 예외 조건과 프로젝트별 조정 사항.
- 참고 출처, 확인일, 미결정 사항.

`필수`, `권장`, `선택`의 강도를 구분하고 “적절히”, “가능하면”만으로 규칙을 끝내지 않는다. 예시는 짧고 실용적으로 작성하며, 문법 오류 예시와 스타일 위반 예시를 명확히 구별한다.

하나의 규칙은 주된 정의 위치를 하나만 둔다. 다른 문서에서는 링크나 짧은 요약을 사용한다. 폴더 구조를 재편할 때는 탐색용 문서와 기존 링크도 함께 갱신한다.

## 검증과 완료 보고

- 변경한 규칙이 다른 문서 및 예제와 모순되지 않는지 확인한다.
- 로컬 링크와 경로를 확인하고 외부 자료의 확인 여부를 정확히 기록한다.
- 코드 예제가 지정한 언어 버전과 맞는지 검토한다. 실행 가능한 예제의 동작이 핵심이라면 사용 가능한 환경에서 컴파일·실행한다.
- `.clang-format`, `.clang-tidy` 등 도구 설정은 관련 규칙이 결정되고 작업 범위에 포함되었을 때 작성한다. 도구 버전과 실제 지원 옵션을 확인한다.
- 문서만 수정한 경우 불필요한 테스트 환경을 만들지 않는다. 실행하지 않은 검증을 완료했다고 보고하지 않는다.
- 완료 시 변경한 파일, 주요 결정, 검증 결과와 남은 미결정 사항을 간결하게 알린다.
