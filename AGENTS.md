# 컨벤션 저장소 작업 지침

- 저장소 안내는 [README.md](README.md), 분류는 [전체 목차](docs/index.md), 채택된 규칙은 [컨벤션 적용 기준](docs/baseline.md)을 따른다.
- 관련 기준 문서와 `ref/`의 기존 근거를 먼저 읽는다. `ref/` 원본은 보존하고, 파일명·수정 시각만으로 우선순위를 정하지 않는다.
- 명확한 기존 선호를 유지한다. 새 제안은 `초안`으로 표시하고 사용자 선택이나 명확한 기존 근거 없이 `채택`하지 않는다. `보류`·`폐기` 및 미결정 사항을 구분한다.
- 규칙을 개정하면 해당 문서와 `docs/baseline.md`의 변경 기록을 갱신한다. 출처의 권고와 개인 규칙을 구분하고, 차이와 채택 이유를 적는다.
- 외부 규칙 도입 시 공식 문서·원저자 가이드를 확인하고 관련 절 링크, 확인일, 명세 버전을 기록한다. 필요한 내용만 한국어로 요약한다.
- 문서는 `docs/` 아래 주제별 Markdown으로 작성한다. 설명은 한국어, 식별자·명령어·API는 원래 표기를 사용한다. 경로는 영문 소문자 `kebab-case`, 링크는 `/` 구분자의 상대 경로로 쓴다.
- 규칙의 상세 정의는 한 곳에 둔다. 새 페이지에는 상위 목차·관련 문서 링크를 넣고 분류 목차와 `mkdocs.yml`을 갱신한다. 이동 시 기존 링크도 수정한다.
- 규칙에는 적용 범위·상태·이유·예외·필요한 예시를 적고 `필수`·`권장`·`선택`을 구분한다. C와 C++, 언어 버전, 플랫폼·프로젝트 제약을 구분하며 미정 사항을 확정하지 않는다. 성능 주장은 적용 조건과 근거를 명시한다.
- 사이트는 기존 Markdown을 원본으로 사용한다. 도구 설정은 관련 규칙이 결정되고 요청 범위에 포함될 때만 변경한다.
- 변경 후 문서·예제의 일관성과 로컬 링크를 확인한다. 사이트 구성 변경 시 README의 빌드 검증을 수행하고, 예제 동작이 핵심이면 지원 환경에서 컴파일·실행한다. 완료 시 변경 파일·검증 결과·미결정 사항을 간결히 보고한다.
- 컨벤션 편집 요청만으로 커밋·푸시·이력 재작성·저장소 설정 변경을 수행하지 않는다.
